# Output File Data

## Other Info
 * Uses Binary

## Header Stuff
 * Layers count `size_t`
 * Each of the layers size `size_t`

## Body Stuff
All of the layer weights will just be yeeted in every other:
 * Weights `double`
 * Change `double`
